你的任务是对指定范围的代码进行架构级审查。按以下步骤执行：

1. 确定审查范围：
   - 如果给了文件路径，直接审查
   - 如果没有，用 `git diff --name-only HEAD~1` 获取最近改动的文件列表
2. 逐文件审查，关注以下维度：
   - **危险区影响**：是否修改了 shared/router/interceptor/config，如涉及则 `cat docs/specs/11_DANGEROUS_AREAS.md`
   - **重复造轮子**：项目中是否已有同类实现（搜索确认）
   - **分层违规**：页面是否裸调接口、组件是否直接操作 store、service 是否混入 UI 逻辑
   - **类型安全**：是否有 `any`、类型断言、缺失的返回类型
   - **状态与副作用**：useEffect 依赖是否正确、是否有内存泄漏风险
3. 输出审查结论：

```
风险等级：🟢 低 / 🟡 中 / 🔴 高

问题清单：
- [文件:行号] 问题描述 → 建议修改方式

确认项：
- 需要人工确认的业务逻辑或边界条件
```

审查对象：$ARGUMENTS
