你的任务是排查一个问题。按以下步骤执行：

1. 先分类问题层级：渲染层 / 数据层 / 接口层 / 配置层 / 构建层
2. 收集证据：查看报错信息、相关代码、网络请求
3. 如果涉及公共模块，先 `cat docs/specs/11_DANGEROUS_AREAS.md` 确认影响面
4. 列出可能根因（不超过 3 个），每个附验证方法
5. 选择最可能的根因，实施修复
6. 修复后运行 CLAUDE.md 中声明的测试命令验证修复
7. 列出回归范围：哪些页面/功能可能受影响

不要把没有证据的猜测包装成结论。

问题描述：$ARGUMENTS
