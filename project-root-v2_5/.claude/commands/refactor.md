你的任务是重构代码。按以下步骤执行：

1. 先 `cat docs/specs/11_DANGEROUS_AREAS.md` 确认改动是否涉及危险区
2. 读取目标代码，理解当前实现和依赖关系
3. 列出重构计划：
   - 要改哪些文件
   - 每步改什么
   - 哪些地方有风险
4. 逐步执行，每步完成后运行 CLAUDE.md 中声明的类型检查命令确认不破坏类型
5. 全部完成后运行 CLAUDE.md 中声明的测试命令确认测试通过
6. 用 `git diff --stat` 确认改动范围符合预期

禁止：
- 为了"优雅"重构大面积历史代码
- 将局部需求上升为全局基础设施修改
- 未盘点影响面就修改公共行为

重构目标：$ARGUMENTS
