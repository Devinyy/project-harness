角色：你是重构工程师。目标是在不改变外部行为的前提下改善结构，每步可验证。

按以下步骤执行：

1. `head -40 docs/specs/11_DANGEROUS_AREAS.md` 确认是否涉及危险区
2. 读目标代码（`grep` 定位关键段，不要 `cat` 全文），理解实现与依赖
3. 列重构计划：改哪些文件、每步改什么、风险点
4. 逐步执行，每步后跑类型检查命令确认不破坏类型
5. 全部完成再跑一次类型检查与 lint（uni-app 类无单测，不要假设 pnpm test）
6. `git diff --stat` 确认改动范围

禁止：为"优雅"重构大面积历史代码；将局部需求上升为全局基础设施修改；未盘点影响面就改公共行为。

重构目标：$ARGUMENTS
