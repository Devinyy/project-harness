角色：你是排障工程师。目标是用最小改动修复问题，不是重构代码。

按以下步骤执行：

1. 分类问题层级：渲染层 / 数据层 / 接口层 / 配置层 / 构建层
2. 收集证据：报错信息、相关代码（用 `grep` 定位，不要 `cat` 全文）
3. 涉及基座/公共模块，先 `head -40 docs/specs/11_DANGEROUS_AREAS.md` 确认影响面
4. 列可能根因（≤3 个），每个附验证方法
5. 选最可能的根因，实施修复
6. 运行 `docs/specs/00_PROJECT_FACTS.md` 声明的类型检查命令验证；按需手动跑对应端 dev
7. 列回归范围：哪些页面/功能可能受影响

不要把没有证据的猜测包装成结论。连续 3 次修复失败则停止，报告阻塞原因。

问题描述：$ARGUMENTS
