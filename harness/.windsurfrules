# Project Harness（大厨管家 · 通用 · specs 外置）

开始前先读 `AGENTS.md` 与 `CLAUDE.md`。只有 `active` 的 `docs/specs/` 才是项目事实：
- `active`：facts 存在、无占位符、文档状态均为 `active` → 先读 facts 与 `INDEX.md`，其余按需查阅。
- `draft`：仍有占位符、`draft/template` 状态或 facts 未声明 `active` → 先按 `_RULE.md` 填充并复核，不得当作事实。
- `missing`：facts 不存在 → 跑 `bash scripts/init-specs.sh`，再填充并复核。

团队两类项目（harness 通用，由 specs 区分）：PC 微前端类（Vue3 + micro-app + dcgj-ui + @platform/http-client）；uni-app 多端类（uni-app + Vue3 + uview-plus，`#ifdef/#ifndef`）。

通用禁止：React / 非约定 UI 库；裸调接口（走项目的请求封装）；为局部需求改公共/基座/请求封装/认证/全局配置。项目专属禁止项见 `docs/specs/00_PROJECT_FACTS.md`。

不要把示例当成项目事实，以真实代码为准。涉及路由/权限/请求/构建/基座时先读 `docs/specs/11_DANGEROUS_AREAS.md`。
